# Archivos solo de desarrollo local (no se publican)
.devserver.ps1
.claude/
firebase-debug.log
*.log

# Configuración con secretos (API keys) — no versionar, sí se despliega
crm/config.local.js
.firebase/
